class Solution {
public:
    int minStartValue(vector<int>& nums) {
        int current_sum = 0;
        int min_sum = 0;
        for (int num : nums) {
            current_sum += num;
            min_sum = min(min_sum, current_sum);
        }
        return max(1, 1 - min_sum);
    }
};
